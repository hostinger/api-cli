## hostinger domains availability suggest-names-from

Suggest domain names from a domain

### Synopsis

Suggest available domain names based on a domain name you already have in mind.

Suggestions are generated by an AI model, so they differ between calls.

Endpoint has rate limit of 90 requests per minute.

Use this endpoint when the domain you wanted is taken and you need close alternatives.

```
hostinger domains availability suggest-names-from [flags]
```

### Options

```
      --domain string   Domain name to base the suggestions on
  -h, --help            help for suggest-names-from
      --limit int       Amount of domain names to suggest
```

### Options inherited from parent commands

```
      --config string   Config file (default is $HOME/.hostinger.yaml)
      --format string   Output format type (json|table|tree), default: table
```

### SEE ALSO

* [hostinger domains availability](hostinger_domains_availability.md)	 - Availability commands

