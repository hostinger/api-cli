## hapi hosting domains delete-website-subdomain

Delete website subdomain

### Synopsis

Delete an existing subdomain from the selected website.

Use this endpoint to remove subdomains that are no longer needed.

```
hapi hosting domains delete-website-subdomain <username> <domain> <subdomain> [flags]
```

### Options

```
  -h, --help   help for delete-website-subdomain
```

### Options inherited from parent commands

```
      --config string   Config file (default is $HOME/.hapi.yaml)
      --format string   Output format type (json|table|tree), default: table
```

### SEE ALSO

* [hapi hosting domains](hapi_hosting_domains.md)	 - Domains commands

